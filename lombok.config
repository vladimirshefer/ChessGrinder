# To avoid runtime checks in lombok
# Lombok treats this annotation as lombok.NonNull
# See https://github.com/projectlombok/lombok/issues/2078
# This does not actually fix enything, but rather tries to shelve the problem.
lombok.copyableAnnotations -= jakarta.annotation.Nullable
